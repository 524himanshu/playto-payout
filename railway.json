{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py migrate && python manage.py shell -c \"from payouts.seed import run; run()\" ; gunicorn config.wsgi:application --bind 0.0.0.0:8080",
    "restartPolicyType": "ON_FAILURE"
  }
}